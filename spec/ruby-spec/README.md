These tests are adapted copies of ruby/spec tests for SortedSet.

Prefer updating with `rake sync_ruby_spec` instead of editing directly.

Tests for #classify, #divide, #select, #reject are manually modified, though.
